import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  video: {
    width: '100%',
    aspectRatio: 16 / 9,
  },
  poster: {
    width: '100%',
    aspectRatio: 16 / 9,
    resizeMode: 'cover',
  },
  buttons: {},
});

export default styles;
